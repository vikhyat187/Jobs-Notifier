<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vikhyat.jobnotifier">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JobNotifier">

        <activity android:name="Activities.RegisterActivity" />
        <activity android:name="Activities.LoginActivity" />
        <activity android:name=".JobsActivity" >

        </activity>
        <activity android:name=".ServiceBasedActivity" />
        <activity android:name=".ProductBasedActivity" />
        <activity android:name=".UserLoggedIn" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>